#include "main.h"
/**
 * main - writ the programs name
 * @ac: number of array arg
 * @av: arguments
 * Return: 0 if Success
 */
int main(int ac, char **av)
{
	(void)ac;

	printf("%s\n", av[0]);
	return (0);
}
