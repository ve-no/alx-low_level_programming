#include "main.h"

/**
 * main - writ the programs name
 * @argc: number of array arg
 * @argv: arguments
 * Return: 0 if Success
 */
int main(int ac, char **av)
{
	(void)ac;
	printf("%s\n", av[0]);
	return (0);
}
